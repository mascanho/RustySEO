// export const dynamic = "force-static";
import "@mantine/core/styles.css";
import { Roboto } from "next/font/google";
import "./globals.css";
import { MantineProvider } from "@mantine/core";
import MenuDrawer from "./components/ui/MenuDrawer";
import TopMenuBar from "./components/ui/TopMenuBar";

import { Toaster } from "@/components/ui/sonner";
import Footer from "./components/ui/Footer";

const roboto = Roboto({
  subsets: ["latin"],
  weight: ["400", "700"],
  variable: "--font-roboto",
});

// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// };

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="min-w-[600px]">
      <body
        className={`relative overflow-hidden rounded-md bg-gray-100 dark:bg-brand-dark ${roboto.className}`}
      >
        <MantineProvider>
          {/* Fixed MenuDrawer */}
          <section className="mb-[6.7rem]">
            <TopMenuBar />
          </section>
          <main className="mt-10   rounded-md  ">
            {children}
            <Toaster />
          </main>
          <Footer />
        </MantineProvider>
      </body>
    </html>
  );
}
